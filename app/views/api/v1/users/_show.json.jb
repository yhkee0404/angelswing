datetime_format_string = "%Y-%m-%dT%H:%M%z"

json = {
  token: "#{response.headers['Access-Token']} (JWT)",
  email: user.email,
  name: "#{user.first_name} #{user.last_name}",
  country: user.country,
  createdAt: user.created_at.localtime.strftime(datetime_format_string),
  updatedAt: user.updated_at.localtime.strftime(datetime_format_string)
}

json